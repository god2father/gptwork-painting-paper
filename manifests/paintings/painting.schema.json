{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "painting.schema.json",
  "type": "object",
  "required": ["id", "title", "subtitle", "duration", "canvas", "background", "layers"],
  "properties": {
    "id": { "type": "string", "minLength": 1 },
    "title": { "type": "string", "minLength": 1 },
    "subtitle": { "type": "string", "minLength": 1 },
    "duration": { "type": "number", "exclusiveMinimum": 0 },
    "canvas": {
      "type": "object",
      "required": ["width", "height"],
      "properties": {
        "width": { "type": "number", "exclusiveMinimum": 0 },
        "height": { "type": "number", "exclusiveMinimum": 0 }
      },
      "additionalProperties": false
    },
    "background": {
      "type": "object",
      "required": ["src", "alt"],
      "properties": {
        "src": { "type": "string", "minLength": 1 },
        "alt": { "type": "string", "minLength": 1 }
      },
      "additionalProperties": false
    },
    "layers": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/layer" }
    }
  },
  "$defs": {
    "transform": {
      "type": "object",
      "required": ["x", "y", "scale", "rotation", "opacity"],
      "properties": {
        "x": { "type": "number" },
        "y": { "type": "number" },
        "scale": { "type": "number", "exclusiveMinimum": 0 },
        "rotation": { "type": "number" },
        "opacity": { "type": "number", "minimum": 0, "maximum": 1 }
      },
      "additionalProperties": false
    },
    "layer": {
      "type": "object",
      "required": ["id", "name", "src", "alt", "description", "z", "bounds", "collapsed", "expanded", "animation", "parallax"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "name": { "type": "string", "minLength": 1 },
        "src": { "type": "string", "minLength": 1 },
        "alt": { "type": "string", "minLength": 1 },
        "description": { "type": "string", "minLength": 1 },
        "z": { "type": "number" },
        "bounds": {
          "type": "object",
          "required": ["x", "y", "width", "height"],
          "properties": {
            "x": { "type": "number", "minimum": 0 },
            "y": { "type": "number", "minimum": 0 },
            "width": { "type": "number", "exclusiveMinimum": 0 },
            "height": { "type": "number", "exclusiveMinimum": 0 }
          },
          "additionalProperties": false
        },
        "collapsed": { "$ref": "#/$defs/transform" },
        "expanded": { "$ref": "#/$defs/transform" },
        "animation": {
          "type": "object",
          "required": ["start", "duration", "ease"],
          "properties": {
            "start": { "type": "number", "minimum": 0, "maximum": 1 },
            "duration": { "type": "number", "exclusiveMinimum": 0, "maximum": 1 },
            "ease": { "type": "string", "minLength": 1 }
          },
          "additionalProperties": false
        },
        "parallax": {
          "type": "object",
          "required": ["x", "y"],
          "properties": { "x": { "type": "number" }, "y": { "type": "number" } },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
